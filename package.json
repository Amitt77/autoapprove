{
  "name": "slack-pr-bot",
  "version": "1.0.0",
  "description": "Auto-approves GitHub PRs from whitelisted Slack users via Cloudflare Workers",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
